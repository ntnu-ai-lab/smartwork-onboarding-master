(function(window) {
  window.env = window.env || {};
  window.env.STATUS_URL = "${STATUS_URL}";
  window.env.ELIGIBILITY_URL = "${ELIGIBILITY_URL}";
  window.env.REGISTER_URL = "${REGISTER_URL}";
})(this);
